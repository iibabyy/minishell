#include "parsing_utils.h"

/*
This function transform tokens to a binary tree
while checking if there is any errors
*/
t_command	*token_to_ast(char *input, t_token *token)
{
	t_command	*command;
	int			
	
}
